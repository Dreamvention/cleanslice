import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cleanslice.app',
  appName: 'cleanslice-app-mobile',
  webDir: './www',
  bundledWebRuntime: false,
  // server: {
  //   androidScheme: 'https',
  // },
};

export default config;
